# cbindgen configuration for hwledger-ffi
# Generated header will be: hwledger.h (C99 standard, C-compatible)

language = "C"
cpp_compat = false

[parse]
# Narrow dependencies to avoid cbindgen parsing issues with f32/f64
parse_deps = false
include = ["hwledger-ffi"]

[export]
prefix = "hwledger_"
