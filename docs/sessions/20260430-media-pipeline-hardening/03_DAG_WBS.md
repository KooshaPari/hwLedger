# DAG WBS

1. Rehydrate GUI screenshots through the native harness.
2. Normalize GUI sync into `public/gui-journeys`.
3. Add media coverage audit and wire it into docs build.
4. Fill missing CLI keyframe coverage for `hf-search-deepseek`.
5. Harden AVSpeech/TTS conversion away from broken `ffmpeg`.
6. Re-run Remotion when Chromium can launch outside the nested sandbox.
