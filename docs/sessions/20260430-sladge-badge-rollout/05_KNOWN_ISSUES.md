# Known Issues

The canonical `hwLedger` checkout still has unrelated local changes. The badge
commit is prepared on `probe/hwledger-lfs-state` and should be integrated only
after those unrelated edits are handled or deliberately merged.

