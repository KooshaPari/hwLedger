# Implementation Strategy

## Approach

Use an isolated worktree so the badge rollout does not mix with unrelated
canonical checkout edits. Keep the implementation to README metadata plus
session documentation.

