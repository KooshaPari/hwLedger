# Research

## Repository Fit

hwLedger describes itself as an LLM capacity planner, fleet ledger, and desktop
inference runtime. Its README references MLX, mistral.rs, llama.cpp, vLLM, TGI,
GGUF, Ollama, local Apple Silicon inference, and a desktop app plus
agent/server pair.

## LFS State

A fresh isolated worktree created from current `HEAD` with
`GIT_LFS_SKIP_SMUDGE=1` reported clean `git status` and clean `git lfs status`.
The previous LFS pointer/smudge blocker did not reproduce.

